# Test files

This directory contains test files for the TFD Builds application.

## Running Tests

- `npm test` - Run all tests once
- `npm run test:watch` - Run tests in watch mode

## Test Structure

Tests are organized by functionality and use Vitest as the testing framework.
